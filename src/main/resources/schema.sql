CREATE TABLE employee(
                         id BIGINT PRIMARY KEY,
                         name VARCHAR(100),
                         city VARCHAR(100),
                         salary DOUBLE,
                         department VARCHAR(50)
);